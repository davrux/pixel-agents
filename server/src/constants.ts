/** Key stamped onto the bundled default layout (used by assetLoader). */
export const LAYOUT_REVISION_KEY = 'layoutRevision';

/** Claude tool classification, mirrored to the client via providerCapabilities. */
export const READING_TOOLS = ['Read', 'Grep', 'Glob', 'WebFetch', 'WebSearch'];
export const SUBAGENT_TOOL_NAMES = ['Task', 'Agent'];
