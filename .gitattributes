# Auto-detect text files and normalize line endings to LF
* text=auto eol=lf

# Binary assets — do not diff or merge
*.png binary
*.jpg binary
*.ttf binary

# DOS game config files — keep original CRLF line endings for the emulator.
assets/cfg/*.cfg -text
